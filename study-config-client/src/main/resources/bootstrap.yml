server:
  port: 8080
spring:
  application:
    name: configclient
  cloud:
    config:
      name: default, @config-project.version@
      profile: @config-project.label.version@
      discovery:
        service-id: configserver
        enabled: true