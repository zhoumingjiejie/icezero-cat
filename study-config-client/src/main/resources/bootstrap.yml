server:
  port: 8080
spring:
  application:
    name: configclient
  cloud:
    config:
      name: guangdong
      profile: dev, auth-dev
      discovery:
        service-id: configserver
        enabled: true