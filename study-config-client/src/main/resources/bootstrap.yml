server:
  port: 8080
spring:
  application:
    name: configclient
  cloud:
    config:
      name: configclient, guangdong
      profile: dev
      discovery:
        service-id: configserver
        enabled: true